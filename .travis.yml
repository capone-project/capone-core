language: c

sudo: false

matrix:
    include:
        - os: linux
          compiler: gcc
        - os: linux
          compiler: clang
        - os: osx
          compiler: gcc
        - os: osx
          compiler: clang

install:
    - ./scripts/deps.sh

script:
    - ./scripts/build.sh
    - ./source/sd/build/sd-test --verbose
